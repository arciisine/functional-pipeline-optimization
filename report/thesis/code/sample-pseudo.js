function findCommonWords(text, threshold) {
  // Split text by non alphanumeric characters
  // For word in words
  //    Skip if the word length is less than 3 (ignore small words)
  //    Convert the word to lower case
  //    Increment counter for word
  //    If counter passes threshold
  //      Mark word as valid
  // Return all valid words
}